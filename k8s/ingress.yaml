apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: stock-market-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: public-nginx
  rules:
    - host: stock-market.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: stock-market
                port:
                  number: 8085
